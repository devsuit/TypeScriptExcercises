function make_shirt(size: string, message: string): void {
  console.log(`You ordered a ${size} shirt with the message: "${message}"`);
}

// Calling the function
make_shirt("Medium", "I heart TypeScript!");
